{
  "name": "moog-muse-interface",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based control panel and patch librarian for the Moog Muse, with JUCE AU/VST3/Standalone wrappers.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:native": "npm run build && cmake -B native/build -S native && cmake --build native/build --config Release"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
